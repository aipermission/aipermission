{
  "name": "aipermission-monorepo",
  "private": true,
  "description": "Local permission gateway for AI agents.",
  "license": "AGPL-3.0-only",
  "workspaces": [
    "frontend",
    "packages/mcp",
    "packages/npm-placeholder"
  ],
  "scripts": {
    "test": "make test",
    "build": "make build",
    "audit": "make audit",
    "hygiene": "node scripts/secret-scan.js && node scripts/line-ending-check.js && node scripts/maintenance-budget-check.js && node scripts/release-version.js --check && node scripts/native-dependency-check.js",
    "version:check": "node scripts/release-version.js --check",
    "version:set": "node scripts/release-version.js --set",
    "release-check": "make release-check"
  }
}
